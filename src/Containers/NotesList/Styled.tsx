import { styled } from '@material-ui/styles';

export const NotesListContainerDiv = styled('div')
({
    padding: 0,
    height: '100%',
    flexDirection: 'row',
    display: 'flex'
});

export const NotesTitleListContainerDiv = styled('div')({
    flex: 0.3,
    flexDirection: 'column',
    display: 'flex'
});

export const MarkdownContainerDiv = styled('div')({
    flex: 0.7,
});
